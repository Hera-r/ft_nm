Ce sujet demande de recoder l'utilitaire UNIX nm en C, sans option, sous le nom ft_nm. Voici une explication détaillée des exigences :

📌 Objectif du projet
Reproduire le comportement de la commande nm, qui sert à afficher la table des symboles des fichiers binaires (notamment les exécutables ELF). Vous devez gérer différents types de fichiers :

x86_32 (architecture 32 bits)
x86_64 (architecture 64 bits)
Object files (.o)
Shared libraries (.so)
Vous devez vous assurer que votre programme affiche les symboles de manière identique à nm, en termes d’ordre, d’offset et de padding.

🛠️ Contraintes et directives
Nom du programme : ft_nm
Fichiers à rendre : Makefile et tout le code source nécessaire
Makefile : Obligatoire, doit inclure les règles standards (compilation, nettoyage…)
Langage : C uniquement
Bibliothèque autorisée : Vous pouvez utiliser votre propre libft. Elle doit être incluse dans le rendu et son Makefile doit permettre de la compiler.
Fonctions système autorisées :
Gestion des fichiers : open(2), close(2), mmap(2), munmap(2), fstat(2)
Gestion mémoire : malloc(3), free(3)
Gestion des erreurs : exit(3), perror(3), strerror(1)
Écriture : write(2)

🔎 Aspects techniques importants
Vous devez travailler sous Linux avec une machine virtuelle ou Docker.
Utiliser mmap pour lire efficacement les fichiers binaires.
Manipuler les fichiers ELF en lisant leur structure correctement (header ELF, sections, symboles…).
Comparer vos résultats avec nm pour vous assurer de la validité de votre sortie.
Vérifier la cohérence des données lues, car certaines valeurs peuvent être corrompues ou mal formatées.

🚨 Gestion des erreurs
Aucune sortie imprévue ! Pas de segmentation fault, bus error, double free, etc.
Vérifier toutes les données avant de les utiliser (exemple : offsets corrects, chaînes bien terminées, architectures respectées).
Gérer les fichiers ELF incorrects ou mal formés proprement.

📜 Résumé final
Vous devez créer un programme ft_nm en C qui reproduit nm en lisant les fichiers ELF (exécutables, objets, bibliothèques partagées). Vous utiliserez mmap pour gérer efficacement la lecture des fichiers, et vous devrez prendre en charge les architectures x86_32 et x86_64. Votre sortie devra être identique à nm, et votre code devra être robuste face aux erreurs.

🔹 Conseil : Consultez la page de manuel man nm et documentez-vous sur la structure des fichiers ELF (man elf).
